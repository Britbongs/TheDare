#ifndef ENEMY_H
#define ENEMY_H
#include "Entity.h"
class Enemy
	: public Entity
{
public:
	Enemy();
	
	float getMoveSpeed() const { return moveSpeed; }

	void update(const sf::Time&);
	
private:
	float moveSpeed;
	bool alive;
private:

	void updateMovement(const sf::Time&);

};
#endif